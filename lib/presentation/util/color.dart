import 'dart:ui';

import 'package:flutter/material.dart';

class ClolorManager {
  static final colorManager = ThemeData(primaryColor: primary);
  static const primary = Color.fromRGBO(31, 162, 203, 1);
  static const disPrimary = Color.fromRGBO(254, 197, 111, 1);
}
